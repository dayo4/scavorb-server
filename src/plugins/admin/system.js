// const app = require('../fastify')

module.exports = {
    settings: {} /*  Contains the default system settings and rules.
                This will be set in the server.js file before system startup. */

}